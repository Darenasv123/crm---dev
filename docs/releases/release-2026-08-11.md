# Candidato de release CRM — 2026-08-11

## Alcance

Este candidato consolida en Git las mejoras funcionales del CRM relativas a:

- tareas por fecha, inicio y finalización;
- ficha de cliente y vistas relacionadas;
- filtros de clientes y expedientes;
- edición de metadatos y catálogo de tipos documentales;
- recuperación y restablecimiento de contraseña;
- importación ZIP con análisis, vista previa, simulación y controles de seguridad;
- contrato PostgreSQL requerido por esas funciones;
- pruebas y tooling de validación del release.

## Contrato de base de datos

La migración requerida es
`supabase/migrations/20260806120000_crm_daily_tasks_and_document_integrity.sql`.
El bootstrap canónico self-hosted ya representa su estado final equivalente.

No se ha verificado ni aplicado esta migración en Supabase Cloud productivo.
Por tanto, este candidato no debe empaquetarse ni desplegarse hasta confirmar
que Cloud contiene el contrato requerido, en particular `case_tasks.scheduled_for`,
los historiales de tareas y documentos, y sus funciones, triggers y políticas.

## Configuración operativa pendiente

- Auth: confirmar Site URL, redirect allowlist para `/restablecer-contrasena` y SMTP.
- Importador ZIP: el límite de aplicación es 1 GiB; falta validar en un entorno
  equivalente que proxy, runtime Node, memoria, tiempo de petición y Storage
  admitan de forma segura una carga de ese tamaño.
- Mantener las URL y claves actuales de Supabase Cloud hasta una migración
  self-hosted autorizada por separado.

## Validación

El candidato sólo se considera reproducible cuando, desde su commit y en un
checkout limpio con dependencias instaladas desde el lockfile, pasan:

- suite completa de pruebas;
- TypeScript;
- ESLint;
- build de producción Node;
- smoke test del servidor construido.

## Estado de entrega

No generar ZIP, no desplegar y no ejecutar SQL remoto durante esta fase.
El estado previsto tras validar el checkout limpio es
`RELEASE_REPRODUCIBLE_WAITING_CLOUD`; cualquier fallo produce
`RELEASE_BLOCKED`.
